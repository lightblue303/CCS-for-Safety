<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 상단 바 -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/topAppBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#1E1E1E"
        android:title="Hand Tracking"
        android:titleTextColor="@android:color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 손 감지 상태 바 -->
    <include
        android:id="@+id/handStatusBar"
        layout="@layout/toolbar_hand_status"
        app:layout_constraintTop_toBottomOf="@id/topAppBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 영상 + 오버레이 영역 -->
    <FrameLayout
        android:id="@+id/cameraContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/handStatusBar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- ESP32-CAM 영상 -->
        <ImageView
            android:id="@+id/previewImageView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitCenter"/>

        <!-- 손 랜드마크 오버레이 -->
        <com.example.image_ai.ui.HandOverlayView
            android:id="@+id/handOverlayView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <!-- 버튼 -->
        <Button
            android:id="@+id/btnMotorRun"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_marginTop="70dp"
            android:layout_marginEnd="16dp"
            android:background="#1E1E1E"
            android:text="모터 재시작"
            android:textColor="#FFFFFF" />

        <!-- 버튼 -->
        <Button
            android:id="@+id/btnSetDangerZone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_marginTop="130dp"
            android:layout_marginEnd="16dp"
            android:background="#1E1E1E"
            android:text="구역 설정"
            android:textColor="#FFFFFF" />
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>